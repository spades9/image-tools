
export struct ImageList {
    name: string,
    path: string,
    status: int
}


export global CompressGlobal {
    //选择的文件夹路径
    in-out property <string> compress-path : "e://rust";

    //存放数据
    in-out property <[ImageList]> image-list : [];

    //打开文件夹
    callback open-folder();

    //批量处理
    callback compress-batching();
}