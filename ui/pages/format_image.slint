import {Content} from "../common/content.slint";

export component FormatImage inherits Rectangle { 

    Content {
        Rectangle {
            Text {
                text: "转换图片";
            }
        }
    }

}

