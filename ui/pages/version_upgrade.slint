import {Content} from "../common/content.slint";

export component VersionUpgrade inherits Rectangle { 

    Content {
        Rectangle {
            Text {
                text: "版本升级";
            }
        }
    }

}

